services:
  app:
    build: .
    container_name: synapsis_app
    ports:
      - "4657:4657"
    environment:
      SECRET_KEY_BASE: "change-me-in-production-use-mix-phx-gen-secret-to-generate"
      SYNAPSIS_ENCRYPTION_KEY: "change-me-32-byte-encryption-key"
      PHX_HOST: localhost
      PORT: "4657"
      MIX_ENV: prod
    volumes:
      - app_data:/app/data

volumes:
  app_data:
